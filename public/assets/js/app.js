import{
    Modal,
    Ripple,
    initTE,
}from "tw-elements";

// initTE({Modal, Ripple});

// window.addEventListener('DOMContentLoaded', (event) => {
//     import { Modal, Ripple, initTE } from 'tw-elements';
//     initTE({ Modal, Ripple });
// });
